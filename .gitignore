node_modules/
dist/
test-results/
*.log
.DS_Store
.prototype-data/
.env
.env.*
!.env.example
!**/.env.example
!.env.services.example
!.env.production.example
*.tsbuildinfo

# Die Expo-App wird vollständig von der Web-Version in apps/web abgelöst.
# Lokal liegt hier nur noch der Export (dist/, .expo/), kein Quellcode.
apps/mobile/

# Die Vergleichsbilder sind 17 MB Einwegmaterial und bleiben lokal.
# Die Zahlen und Beobachtungen stehen im README daneben.
docs/model-comparison/*/

# Opt-in local gallery of garment collages sent to look generation.
apps/web/public/debug/
